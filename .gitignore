# Dependências / saída de build
node_modules/
dist/

# Ferramentas e artefatos internos (não fazem parte do produto publicado)
.claude/
_bmad/
_bmad-output/

# Dado REAL do Salesforce — NUNCA versionar (só fictício é público). Fase 3 / porteiro.
/private/
*-real.json
snapshot_leads_*.json

# Wrangler — cache/estado local da conta Cloudflare (não versionar)
.wrangler/

# Sistema / logs
.DS_Store
Thumbs.db
*.log

# Deck da supervisao: material interno, e o Pages serve a raiz — nao versionar.
# (Entrou por engano em 1bbfa4d e foi removido no commit seguinte.)
/apresentacao/
